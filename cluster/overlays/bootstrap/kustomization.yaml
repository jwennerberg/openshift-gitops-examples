apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
    #- github.com/redhat-developer/gitops-operator//config/crd/?ref=v1.4.0
  - github.com/redhat-cop/gitops-catalog/openshift-gitops-operator/overlays/stable/?ref=main
  - application-gitops-hub.yaml

patchesJson6902:
  - target:
      group: config.openshift.io
      version: v1
      kind: OAuth
      name: cluster
    path: oauth-htpasswd-patch.yaml
